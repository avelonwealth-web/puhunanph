# Public Frontend Folder

This folder is reserved for frontend hosting output.

Current project keeps working frontend files at the repository root for compatibility.
If you want to fully switch to `public` publishing, copy all root HTML pages and `assets/` into this directory, then update Netlify publish path to `public`.
